services:
  - type: web
    name: fridge-genius-backend
    env: node
    region: oregon
    plan: free
    rootDir: fridge-genius/backend
    buildCommand: npm install
    startCommand: npm run dev
    envVars:
      - key: PORT
        value: 10000
      - key: GOOGLE_API_KEY
        sync: false
